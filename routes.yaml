routes:
  /:
    data: page.home
    template: index
  /locations/: locations
  /calendar/: calendar
  /community/: community

collections:
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/