<!DOCTYPE html>
<html lang="{{@site.lang}}">
<head>

    {{!-- Document Settings --}}
    <meta charset="utf-8" />
    {{!-- <meta http-equiv="X-UA-Compatible" content="IE=edge" /> --}}

    {{!-- Base Meta --}}
    <title>{{meta_title}}</title>
    {{!-- <meta name="HandheldFriendly" content="True" /> --}}
    {{!-- <meta name="viewport" content="width=1200, initial-scale=1.0" /> --}}
    
    {{!-- Styles'n'Scripts --}}
    <link rel="stylesheet" type="text/css" href="{{asset "built/swiper.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/screen.css"}}" />
    
    <style>
        .swiper-container {
            width: 100%;
        }
        .swiper-button-white {
            color: white;
        }

        .s-list{ height: 82px; line-height: 82px; }
        .s-list li{ margin-top: 24px; margin-right: 52px; padding-right: 52px; vertical-align: middle; line-height: 35px; float: left; border-right: solid 4px #c2c2c2; color: #c2c2c2; font-size: 30px; font-weight: bold; font-family: "å¾®è½¯é›…é»‘"; }
        .s-list li a{  color: #c2c2c2; }
        .s-list li a:hover{ color: #000; }
        .s-info h3{ margin-bottom: 24px; height: 40px; line-height: 40px; background: url('{{asset 'imgs/tbg.jpg'}}'); width: 100%; font-size: 20px; text-indent: 20px; }
        .s-info li{ width: 275px; height: 350px; position: relative; float: left; margin-right: 26px; margin-bottom: 26px; }
        .s-info li img{   }
        .s-info li:nth-child(4n){ margin-right:0; }
        .s-info li p{ position: absolute; height: 170px; color: #fff; bottom: 0; background: url('{{asset 'imgs/ssbg.png'}}') repeat; width: 100%; }
        .s-info li img{ display: block; width: 100%; height: 100%; }
        .s-info li p span{ padding: 137px 0 0 26px; display: block; }
        .jingdian li{ float: left; position: relative; }
        .jingdian li{ width: 396px; height: 801px;  }
        .jingdian li img{ width: 100%; height: 100%; }
        .jingdian .jd1,.jingdian .jd3{ margin-top: 48px; width: 390px; height: 600px; }
        .jingdian span{ width: 100%; height: 100%; position: absolute; top: 0; left: 0; }
        .jingdian .jd1 span{ background: url('{{asset 'imgs/tinning-bg.png'}}'); }
        .jingdian .jd2 span{ background: url('{{asset 'imgs/saiban-bg.png'}}'); }
        .jingdian .jd3 span{ background: url('{{asset 'imgs/luota-bg.png'}}'); }

        .window{ z-index: 1000!important;position: relative; display: none; width: 100%; background: rgba(0,0,0,0.8); position: fixed; top: 0; left: 0; z-index: 4; }
        .win-info{ width: 1180px; margin: 0 auto; padding-top:20px; }
        .w-list{ float: right; width: 180px; overflow: hidden; }
        .w-list ul{  height: 502px; overflow-y: scroll; width: 200px; }
        .w-list li{ width: 180px; height: 100px; padding-bottom: 20px; cursor: pointer; }
        .w-list li img{ width: 100%; height: 100%; }
        .bw-list{ float: left; width: 976px; }
        .bw-list li img{ width: 100%; display: block; max-height: 502px; }
        .bs-list{ width: 1180px; margin: 0 auto; }
        li .w-info{ position: relative; padding-top: 20px; color: #fff; }
        li .w-info h3{ font-size: 30px; padding-left: 25px;  }

        li .w-info .xinxix{ position: absolute; font-size: 20px; top: 29px; right: 25px; }
        li .w-info p{ font-size: 14px;  line-height: 18px; padding: 30px 25px 25px; }
        .bs-list li{ display: none; }
        .bw-list li{ display: none; }
        .bw-list li:nth-child(1){ display: block; }
        .bs-list li:nth-child(1){ display: block; }

        .window .close{ position: absolute; width: 40px; height: 40px; right: 0; top: 0;  color: #000; cursor: pointer; }
        .window .close img{ width: 100%; height: 100%;  }
        .to-top{ position: fixed; display: none; top: 50%; right: 20px; z-index: 99; }
        .gotop{ padding-bottom: 4px; cursor: pointer; }

        /* footer styles */
        .clearfix::after { content: "."; display: block; height: 0; clear: both; visibility: hidden; }
        footer.site-footer { background: #313131!important; padding-top: 0!important;padding-bottom: 0!important;}
        footer a:hover { text-decoration: none; }
        .footer{ width: 1132px; margin: 0 auto; position: relative; }
        .footer h3{ padding-top: 100px; }
        .footer .erweima{ position: absolute; right: 40px; top: 60px; }
        .lianxi li.lx1{  border-left: solid 1px #807c71; }
        .lianxi li{ padding: 0;list-style: none; margin: 45px 0; float: left; width: 362px; height: 76px; border-right: solid 1px #807c71; }
        .lianxi li img{ display: block; margin: -10px auto 0; }
        .lianxi li p{ color: #807c71; font-size: 20px; padding-left: 50px; }
        .lianxi li span{ color: #807c71; font-size: 12px; padding-left: 30px; display: block; }
        .lianxi{ position: relative; margin-top: 80px; }
        .line{ width: 100%; height: 1px; background: #807c71; position: absolute; top: 165px; }
        .women{  color: #807c71; padding-top: 60px; padding-bottom: 30px; padding-left:50px; font-size: 15px; }
        .site-main { margin-top: 80px!important;}

        ul {padding: 0!important;list-style: none!important;}
        .s-info p {margin: 0!important}
    </style>
    {{!-- This tag outputs SEO meta+structured data and other important settings --}}
    {{ghost_head}}

</head>
<body class="{{body_class}}">

    <div class="site-wrapper">

        {{!-- All the main content gets inserted here, index.hbs, post.hbs, etc --}}
        {{{body}}}

        {{!-- The footer at the very bottom of the screen --}}
        <footer class="site-footer outer">
            <div class="footer">
            <h3>
                <img src="{{asset 'imgs/b1.jpg'}}" alt="">
            </h3>
            <div class="erweima"><img src="{{asset 'imgs/b2.jpg'}}" alt=""></div>
            <div class="lianxi clearfix">
                <ul>
                    <li class="lx1 lx2" style="width:217px;"><a href="https://weibo.com/p/1006062776718162/home?from=page_100606&amp;mod=TAB#place"><img src="{{asset 'imgs/b3.jpg'}}" alt=""></a></li>
                    <li class="lx2" style="width:217px;"><a href="http://www.brightentravel.com/"><img src="{{asset 'imgs/b4.jpg'}}" alt=""></a></li>
                    <li class="lx2" style="width:217px;"><a href="https://www.facebook.com/traveltoisland/"><img src="{{asset 'imgs/b5.jpg'}}" alt=""></a></li>
                    <li class="lx2" style="width:217px;"><a href="javascript:void(0);"><img src="{{asset 'imgs/ii_03.png?1'}}" alt="" style="width:180px;margin-top:3px;"></a></li>
                    <li class="lx2" style="width:217px;"><a href="http://www.youwan.com"><img src="{{asset 'imgs/ii_06.png'}}" alt=""></a></li>
                    <li class="lx1"><a href="">
                        <p>北京办公室</p>
                        <span>北京市朝阳区建国门外大街9号齐家园外交公寓8-22室</span>
                        <span>邮编： 100600   电话：(8610) 85324759</span>
                    </a></li>
                    <li><a href="">
                        <p>上海办公室</p>
                        <span>上海市杨浦区宁国路218号A座1805室</span>
                        <span>邮编：200000  电话：(8621) 33688198</span>
                    </a></li>
                    <li><a href="">
                        <p>广州办公室</p>
                        <span>广东省广州市白云区云城西路724号万达广场C3座110室</span>
                        <span>邮编：510420  电话：(8620) 38627665</span>
                    </a></li>
                    <p class="line"></p>
                </ul>
            </div>
            <div class="women">
              <p>联系我们：mva@skywalkergroup.net</p>
              <p>备案号：京ICP备19050420号-1</p>
            </div>
            </div>
        </footer>
    </div>

    {{!-- The big email subscribe modal content --}}
    {{#if @labs.members}}
    <div class="subscribe-success-message">
        <a class="subscribe-close" href="javascript:;"></a>
        You've successfully subscribed to {{@site.title}}!
    </div>

    <div id="subscribe" class="subscribe-overlay">
        <a class="subscribe-close" href="#"></a>
        <div class="subscribe-overlay-content">
            {{#if @site.logo}}
                <img class="subscribe-overlay-logo" src="{{@site.logo}}" alt="{{@site.title}}" />
            {{/if}}
            <div class="subscribe-form">
                <h1 class="subscribe-overlay-title">Subscribe to {{@site.title}}</h1>
                <p class="subscribe-overlay-description">Stay up to date! Get all the latest & greatest posts delivered straight to your inbox</p>
                <form data-members-form="subscribe">
                    <div class="form-group">
                        <input class="subscribe-email" data-members-email placeholder="youremail@example.com"
                            autocomplete="false" />
                        <button class="button primary" type="submit">
                            <span class="button-content">Subscribe</span>
                            <span class="button-loader">{{> "icons/loader"}}</span>
                        </button>
                    </div>
                    <div class="message-success">
                        <strong>Great!</strong> Check your inbox and click the link to confirm your subscription.
                    </div>
                    <div class="message-error">
                        Please enter a valid email address!
                    </div>
                </form>
            </div>
        </div>
    </div>
    {{/if}}

    {{!-- jQuery, required for fitvids --}}
    <script src="{{asset "js/lib/jquery-3.4.1.min.js"}}"></script>
    {{!-- Fitvids (for responsive video embeds), infinite scroll, floating header, and gallery card support --}}
    <script src="{{asset "built/casper.js"}}"></script>

    {{!-- Scripts for Members subscription --}}
    <script>
        // Parse the URL parameter
        function getParameterByName(name, url) {
            if (!url) url = window.location.href;
            name = name.replace(/[\[\]]/g, "\\$&");
            var regex = new RegExp("[?&]" + name + "(=([^&#]*)|&|#|$)"),
                results = regex.exec(url);
            if (!results) return null;
            if (!results[2]) return '';
            return decodeURIComponent(results[2].replace(/\+/g, " "));
        }

        // Give the parameter a variable name
        var action = getParameterByName('action');

        function pagination(c, m) {
            var current = c,
                last = m,
                delta = 2,
                left = current - delta,
                right = current + delta + 1,
                range = [],
                rangeWithDots = [],
                l;

            for (let i = 1; i <= last; i++) {
                if (i == 1 || i == last || i >= left && i < right) {
                    range.push(i);
                }
            }

            for (let i of range) {
                if (l) {
                    if (i - l === 2) {
                        rangeWithDots.push(l + 1);
                    } else if (i - l !== 1) {
                        rangeWithDots.push('...');
                    }
                }
                rangeWithDots.push(i);
                l = i;
            }

            return rangeWithDots;
        }

        function createPagination() {
            var currentPage = parseInt($('.curr-page').text()),
                totalPages = parseInt($('.total-pages').text());
            var url = window.location.href;

            if(totalPages > 1) {
                var paginationArr = pagination(currentPage, totalPages);
                var paginationItem;
                var isCurrent = '';

                for (var i = paginationArr.length - 1; i >= 0; i--) {
                var pageNum = paginationArr[i];

                if (pageNum === currentPage) {
                        paginationItem = '<li class="current">' + pageNum + '</li>';
                    } else if (typeof pageNum === 'number') {
                        var urlArray = url.split('/');
                        if(urlArray[urlArray.length - 3] === 'page') {
                            url = url.replace(/\/page\/.*$/,'') + '/';
                        }
                        paginationItem = '<li>' +
                            '<a href=\"' + url + 'page/' + pageNum + '\" aria-label=\"Page ' + pageNum + '\">'
                            + pageNum + '</a>' +
                            '</li>';
                    } else {
                        paginationItem = '<li class=\"ellipsis\"></li>';
                    }
                    $('.pagination-previous').after(paginationItem);
                }
            } else {
                $('.pagination').css('display', 'none');
            }
        }
        createPagination();
    </script>

    {{!-- The #block helper will pull in data from the #contentFor other template files. In this case, there's some JavaScript which we only want to use in post.hbs, but it needs to be included down here, after jQuery has already loaded. --}}
    {{{block "scripts"}}}

    {{!-- Ghost outputs important scripts and data with this tag - it should always be the very last thing before the closing body tag --}}
    {{ghost_foot}}

</body>
</html>
